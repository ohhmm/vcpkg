diff --git a/configure b/configure
index abc123..def456 789
--- a/configure
+++ b/configure
@@ -123,6 +123,10 @@ function configure_spdk()
     --with-dpdk=*)
         DPDK_DIR="${arg#*=}"
         if [ -d "$DPDK_DIR" ]; then
+            # Override pkg-config search paths to only use local DPDK
+            export PKG_CONFIG_LIBDIR="$DPDK_DIR/lib/x86_64-linux-gnu/pkgconfig"
+            export PKG_CONFIG_PATH="$DPDK_DIR/lib/x86_64-linux-gnu/pkgconfig"
+            unset PKG_CONFIG_SYSTEM_LIBRARY_PATH
             echo "Using DPDK from $DPDK_DIR"
         else
            echo "DPDK directory $DPDK_DIR does not exist"
